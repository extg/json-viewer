{
  "name": "chrome-extools",
  "private": true,
  "scripts": {
    "start": "lerna run start --scope=json-viewer --parallel --stream",
    "build": "lerna run build --scope=json-viewer --parallel --stream",
    "bootstrap": "lerna bootstrap"
  },
  "devDependencies": {
    "lerna": "^3.5.1"
  }
}
